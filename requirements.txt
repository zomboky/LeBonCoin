requests>=2.31
pyyaml>=6.0

# Module `research` (ex-Mach2) : vérification externe des modèles identifiés.
trafilatura>=1.8
beautifulsoup4>=4.12
lxml>=5.0
markdownify>=0.11

# Optionnel : repli navigateur quand DataDome bloque l'API directe, ou pour
# `research --render` sur des sites qui rendent leur contenu en JavaScript.
# playwright>=1.45  (puis `playwright install chromium`)
